# Notes : I change the port here to avoid the conflict of two microservices working on the same port
server.port=8089
#_____________________
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#Database Connection Establish
spring.datasource.username=postgres
spring.datasource.password=moonlightmydata
spring.datasource.url=jdbc:postgresql://localhost:5432/store

spring.datasource.initialization-mode=always
#spring.datasource.schema=classpath:/schema.sql
spring.datasource.continue-on-error=true

#eureka-client.properties
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.serviceUrl.defaultZone  = http://localhost:8761/eureka
eureka.client.instance.hostname = localhost
# (the name that eureka will be call the microservice)
spring.application.name = microservice-category